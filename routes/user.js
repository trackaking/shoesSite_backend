const express = require('express');

const request = require('../database/user');

const router = express.Router();


// route qui permet de retourner les infos du user présentemment connecté
router.get('/', async (req,res) =>{
    
})